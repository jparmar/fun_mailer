<% if @category_images %>
	<% @category_images.each do |photo| %>
		<label>
			<input type="radio" name="categoryImage" data=<%=photo.image_file_name%> />
			<%= image_tag(photo.image.url, class: 'img-thumbnail', size: "100x100") %>
		</label>
	 <% end %>
<% end %>