
<% if @category_images %>
	<% @category_images.each do |photo| %>
		<div class="">
	    	<%= image_tag(photo.image.url, class: 'img-thumbnail', size: "100x100") %>
		</div>
	 <% end %>
<% end %>