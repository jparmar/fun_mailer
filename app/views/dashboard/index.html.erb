<div class="row">
	<div class="col-md-7">
		<div class="btn-group">
			<%#= select_tag "categories", options_from_collection_for_select(@categories, "id", "name"),:class=>'btn btn-default dropdown-toggle' ,:multiple=>false, :prompt=>"Select category" %>
    	<br /><br />
		</div>
	</div>
	<div class="col-md-5">
		
	</div>
	
	<%= form_tag do %>
	<%= select_tag "categories", options_from_collection_for_select(@categories, "id", "name"), :multiple=>false, :prompt =>"Select category", :onchange => "onCategory(this.value)" %>

	<%#= select_tag "quotes", options_from_collection_for_select(@quotes, "id", "name"), :multiple=>true %>
	
	<div id="categoryQuotes">
		<%= render :partial => 'category_quotes', :object => @qoutes %>
	</div>
	
<% end %>
</div>
